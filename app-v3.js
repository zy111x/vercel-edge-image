import { FONT_CATEGORIES, FONT_OPTIONS, ensureFontLoaded, fontById } from './text-fonts.js';
import { TEXT_PRESETS } from './text-presets.js';
import { TextLayerManager } from './text-layer-manager.js';

const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const landingView=$('#landingView'),studioView=$('#studioView'),fileInput=$('#fileInput'),chooseFileButton=$('#chooseFileButton'),dropZone=$('#dropZone'),urlForm=$('#urlForm'),urlInput=$('#urlInput');
const previewCanvas=$('#previewCanvas'),previewImage=$('#previewImage'),canvasStage=$('#canvasStage'),processingMask=$('#processingMask');
const toolPanel=$('#toolPanel'),toolTitle=$('#toolTitle'),toolBadge=$('#toolBadge'),imageName=$('#imageName'),imageDimensions=$('#imageDimensions');
const pipelineChips=$('#pipelineChips'),pipelineCount=$('#pipelineCount'),runButton=$('#runButton'),downloadButton=$('#downloadButton'),exportSummary=$('#exportSummary');
const newImageButton=$('#newImageButton'),compareButton=$('#compareButton'),fitButton=$('#fitButton'),undoButton=$('#undoButton'),redoButton=$('#redoButton'),resetButton=$('#resetButton'),previewMode=$('#previewMode'),toast=$('#toast');

const TOOL_META={resize:['缩放图片','Resize'],crop:['裁剪图片','Crop'],rotate:['旋转与翻转','Rotate'],filter:['滤镜风格','Filter'],adjust:['亮度与对比度','Adjust'],watermark:['图片水印','Watermark'],text:['文字图层','Text Layers'],format:['格式与质量','Export'],pipeline:['处理管线','Pipeline']};
const EFFECT_TOOLS=new Set(['resize','crop','rotate','filter','adjust','watermark']);
const FILTERS=[['oceanic','海洋'],['islands','岛屿'],['marine','深海'],['seagreen','海松'],['flagblue','钴蓝'],['liquid','流光'],['diamante','钻石'],['radio','辐射'],['twenties','二十年代'],['rosetint','玫瑰'],['mauve','淡紫'],['bluechrome','蓝铬'],['vintage','复古'],['perfume','香氛'],['serenity','宁静']];
const FILTER_PREVIEW={oceanic:'saturate(1.15) hue-rotate(155deg) brightness(.96)',islands:'saturate(1.12) hue-rotate(145deg) brightness(1.04)',marine:'saturate(1.25) hue-rotate(125deg) contrast(1.05)',seagreen:'sepia(.16) saturate(1.3) hue-rotate(95deg) brightness(.86)',flagblue:'saturate(1.3) hue-rotate(175deg)',liquid:'saturate(1.4) hue-rotate(165deg) contrast(1.08)',diamante:'saturate(1.2) hue-rotate(145deg) contrast(1.12)',radio:'sepia(.38) saturate(1.5) contrast(1.15) hue-rotate(25deg)',twenties:'sepia(.22) saturate(.8) contrast(.92) hue-rotate(175deg)',rosetint:'sepia(.2) saturate(1.25) hue-rotate(300deg)',mauve:'sepia(.15) saturate(1.2) hue-rotate(245deg)',bluechrome:'grayscale(.35) sepia(.2) saturate(1.5) hue-rotate(175deg)',vintage:'sepia(.38) saturate(.85) contrast(.95) brightness(.96)',perfume:'saturate(1.16) hue-rotate(185deg) brightness(1.04)',serenity:'saturate(.9) hue-rotate(185deg) brightness(1.08)'};
const DEFAULT_OUTPUT={format:'webp',quality:92};
const MAX_PREVIEW_PIXELS=2800000;
const imageCache=new Map();

const state={sourceFile:null,sourceUrl:'',sourceName:'',sourceObjectUrl:'',sourceImage:null,width:0,height:0,activeTool:'resize',pipeline:[],output:{...DEFAULT_OUTPUT},selectedFilter:'vintage',watermarkFile:null,watermarkName:'',watermarkPreviewUrl:'',resizeLock:true,busy:false,panelDirty:false,history:[],historyIndex:-1,restoring:false,renderTimer:null,renderToken:0,resultBlob:null,resultPreview:'',exactMode:false,previewLogicalWidth:0,previewLogicalHeight:0,textFontCategory:'all'};

let textManager;
try {
  textManager=new TextLayerManager({canvasId:'textCanvas',hostId:'textCanvasHost',stageId:'canvasStage',onCommit:()=>{if(!state.restoring){pushHistory();renderPipeline();if(state.activeTool==='text')renderToolPanel();invalidateExact();}},onSelection:()=>{if(state.activeTool==='text'&&!state.restoring)renderToolPanel();},onTransient:()=>{if(state.activeTool==='text'&&!state.restoring)updateTextTransformReadout();}});
} catch (error) {
  console.error(error);
  showToast('文字图层模块加载失败，请刷新页面。','error');
}

const clone=v=>JSON.parse(JSON.stringify(v));
const opName=a=>({resize:'缩放',crop:'裁剪',rotate:'旋转',fliph:'水平翻转',flipv:'垂直翻转',filter:'滤镜',adjust_brightness:'亮度',adjust_contrast:'对比度',watermark:'图片水印'}[a]||a);
const opSummary=o=>{const p=o.params||[];if(o.action==='resize')return `${p[0]} × ${p[1]}`;if(o.action==='crop')return `(${p[0]}, ${p[1]}) → (${p[2]}, ${p[3]})`;if(o.action==='rotate')return `${p[0]}°`;if(o.action==='filter')return String(p[0]||'');if(o.action==='watermark')return `${p[1]}, ${p[2]}`;return p.join(', ')};
let toastTimer;
function showToast(message,type=''){clearTimeout(toastTimer);toast.textContent=message;toast.className=`toast show ${type}`.trim();toastTimer=setTimeout(()=>toast.className='toast',2400)}
function revoke(url){if(url&&url.startsWith('blob:'))URL.revokeObjectURL(url)}
function setBusy(value){state.busy=value;processingMask.hidden=!value;runButton.disabled=value;downloadButton.disabled=value||!state.sourceImage}
function setPreviewMode(exact){state.exactMode=exact;previewMode.classList.toggle('exact',exact);previewMode.innerHTML=`<span></span>${exact?'Photon 精确结果':'浏览器即时预览'}`;if(textManager)textManager.setVisible(Boolean(state.sourceImage)&&!exact)}
function invalidateExact(){if(state.exactMode){state.exactMode=false;previewImage.hidden=true;previewCanvas.hidden=false;setPreviewMode(false);if(textManager)textManager.syncTo(previewCanvas)}}
function updateExport(){const label=state.output.format==='jpg'?'JPEG':state.output.format.toUpperCase();exportSummary.textContent=state.output.format==='png'?`${label} · 无损`:`${label} · ${state.output.quality}%`}

function snapshot(){return {pipeline:clone(state.pipeline),output:{...state.output},text:textManager?.serializeState()||null}}
function sameSnapshot(a,b){return JSON.stringify(a)===JSON.stringify(b)}
function initHistory(){state.history=[snapshot()];state.historyIndex=0;updateHistoryButtons()}
function pushHistory(){if(state.restoring)return;const next=snapshot(),current=state.history[state.historyIndex];if(current&&sameSnapshot(current,next))return;state.history=state.history.slice(0,state.historyIndex+1);state.history.push(next);if(state.history.length>60){state.history.shift();state.historyIndex=59}else state.historyIndex++;updateHistoryButtons()}
function updateHistoryButtons(){undoButton.disabled=state.historyIndex<=0&&!state.panelDirty;redoButton.disabled=state.historyIndex<0||state.historyIndex>=state.history.length-1}
async function restoreSnapshot(index){if(index<0||index>=state.history.length)return;state.restoring=true;state.historyIndex=index;const s=state.history[index];state.pipeline=clone(s.pipeline);state.output={...s.output};state.panelDirty=false;invalidateExact();textManager?.clear({silent:true});renderPipeline();await renderPreview(state.pipeline);await textManager?.loadState(s.text,{silent:true});await textManager?.syncTo(previewCanvas);renderToolPanel();updateHistoryButtons();updateExport();state.restoring=false}
async function undo(){if(state.panelDirty){state.panelDirty=false;renderToolPanel();scheduleLivePreview(0);updateHistoryButtons();showToast('已撤销当前参数调整。');return}if(state.historyIndex>0){await restoreSnapshot(state.historyIndex-1);showToast('已撤销。')}}
async function redo(){if(state.historyIndex<state.history.length-1){await restoreSnapshot(state.historyIndex+1);showToast('已重做。')}}
async function resetEdits(){if(!state.sourceImage)return;if(state.panelDirty||state.pipeline.length||JSON.stringify(state.output)!==JSON.stringify(DEFAULT_OUTPUT)||(textManager?.getObjects().length||0)){state.panelDirty=false;state.pipeline=[];state.output={...DEFAULT_OUTPUT};invalidateExact();textManager?.clear({silent:true});await renderPreview([]);pushHistory();renderPipeline();renderToolPanel();updateExport();showToast('已重置所有处理，可撤销恢复。')}}

function previewSize(w,h){w=Math.max(1,Number(w)||1);h=Math.max(1,Number(h)||1);const scale=Math.min(1,Math.sqrt(MAX_PREVIEW_PIXELS/(w*h)));return {w:Math.max(1,Math.round(w*scale)),h:Math.max(1,Math.round(h*scale)),scale}}
function makeCanvas(w,h){const c=document.createElement('canvas');c.width=Math.max(1,Math.round(w));c.height=Math.max(1,Math.round(h));return c}
function currentLogicalSize(ops=state.pipeline){let w=state.width||1,h=state.height||1;for(const op of ops){const p=op.params||[];if(op.action==='resize'){w=Math.max(1,Number(p[0])||w);h=Math.max(1,Number(p[1])||h)}else if(op.action==='crop'){w=Math.max(1,(Number(p[2])||w)-(Number(p[0])||0));h=Math.max(1,(Number(p[3])||h)-(Number(p[1])||0))}else if(op.action==='rotate'){const rad=(Number(p[0])||0)*Math.PI/180,co=Math.abs(Math.cos(rad)),si=Math.abs(Math.sin(rad));const nw=w*co+h*si,nh=w*si+h*co;w=Math.max(1,Math.round(nw));h=Math.max(1,Math.round(nh))}}return {w:Math.round(w),h:Math.round(h)}}
function drawFiltered(source,filter){const out=makeCanvas(source.width,source.height),ctx=out.getContext('2d');ctx.filter=filter;ctx.drawImage(source,0,0);ctx.filter='none';return out}
function loadImage(url){if(!url)return Promise.reject(new Error('empty image url'));if(imageCache.has(url))return imageCache.get(url);const promise=new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>reject(new Error('无法载入图片'));img.src=url});imageCache.set(url,promise);return promise}
async function applyPreviewOp(work,logical,op,token){const p=op.params||[];if(token!==state.renderToken)return {work,logical};if(op.action==='resize'){const lw=Math.max(1,Math.round(Number(p[0])||logical.w)),lh=Math.max(1,Math.round(Number(p[1])||logical.h)),ps=previewSize(lw,lh),out=makeCanvas(ps.w,ps.h);out.getContext('2d').drawImage(work,0,0,out.width,out.height);return {work:out,logical:{w:lw,h:lh}}}if(op.action==='crop'){const x1=Math.max(0,Math.min(logical.w,Number(p[0])||0)),y1=Math.max(0,Math.min(logical.h,Number(p[1])||0)),x2=Math.max(x1+1,Math.min(logical.w,Number(p[2])||logical.w)),y2=Math.max(y1+1,Math.min(logical.h,Number(p[3])||logical.h));const lw=Math.max(1,x2-x1),lh=Math.max(1,y2-y1),ps=previewSize(lw,lh),out=makeCanvas(ps.w,ps.h),ctx=out.getContext('2d'),sx=work.width/logical.w,sy=work.height/logical.h;ctx.drawImage(work,x1*sx,y1*sy,lw*sx,lh*sy,0,0,out.width,out.height);return {work:out,logical:{w:lw,h:lh}}}if(op.action==='rotate'){const angle=Number(p[0])||0,rad=angle*Math.PI/180,co=Math.abs(Math.cos(rad)),si=Math.abs(Math.sin(rad)),lw=Math.max(1,Math.round(logical.w*co+logical.h*si)),lh=Math.max(1,Math.round(logical.w*si+logical.h*co)),ps=previewSize(lw,lh),out=makeCanvas(ps.w,ps.h),ctx=out.getContext('2d');ctx.translate(out.width/2,out.height/2);ctx.rotate(rad);const scale=Math.min(out.width/lw,out.height/lh);ctx.drawImage(work,-logical.w*scale/2,-logical.h*scale/2,logical.w*scale,logical.h*scale);return {work:out,logical:{w:lw,h:lh}}}if(op.action==='fliph'||op.action==='flipv'){const out=makeCanvas(work.width,work.height),ctx=out.getContext('2d');ctx.translate(op.action==='fliph'?out.width:0,op.action==='flipv'?out.height:0);ctx.scale(op.action==='fliph'?-1:1,op.action==='flipv'?-1:1);ctx.drawImage(work,0,0);return {work:out,logical}}if(op.action==='adjust_brightness'){const v=Math.max(-100,Math.min(100,Number(p[0])||0));return {work:drawFiltered(work,`brightness(${Math.max(.05,1+v/100)})`),logical}}if(op.action==='adjust_contrast'){const v=Math.max(-100,Math.min(100,Number(p[0])||0));return {work:drawFiltered(work,`contrast(${Math.max(.05,1+v/100)})`),logical}}if(op.action==='filter'){return {work:drawFiltered(work,FILTER_PREVIEW[String(p[0])]||'none'),logical}}if(op.action==='watermark'){const src=p[0]==='__watermark__'?state.watermarkPreviewUrl:String(p[0]||'');if(!src)return {work,logical};try{const wm=await loadImage(src);if(token!==state.renderToken)return {work,logical};const out=makeCanvas(work.width,work.height),ctx=out.getContext('2d');ctx.drawImage(work,0,0);const scale=work.width/logical.w,x=(Number(p[1])||0)*scale,y=(Number(p[2])||0)*scale;ctx.globalAlpha=.9;ctx.drawImage(wm,x,y,wm.naturalWidth*scale,wm.naturalHeight*scale);ctx.globalAlpha=1;return {work:out,logical}}catch{return {work,logical}}return {work,logical}}
function getDraftOps(strict=false){if(!state.panelDirty||!EFFECT_TOOLS.has(state.activeTool))return[];return opsFromPanel(strict)}
function effectiveOps(){let draft=[];try{draft=getDraftOps(false)}catch{}return [...state.pipeline,...draft]}
async function renderPreview(ops=effectiveOps()){if(!state.sourceImage)return;const token=++state.renderToken,sourceLogical={w:state.width,h:state.height},ps=previewSize(sourceLogical.w,sourceLogical.h);let work=makeCanvas(ps.w,ps.h),logical={...sourceLogical};work.getContext('2d').drawImage(state.sourceImage,0,0,work.width,work.height);for(const op of ops){const next=await applyPreviewOp(work,logical,op,token);if(token!==state.renderToken)return;work=next.work;logical=next.logical}if(token!==state.renderToken)return;previewCanvas.width=work.width;previewCanvas.height=work.height;previewCanvas.getContext('2d').drawImage(work,0,0);state.previewLogicalWidth=Math.round(logical.w);state.previewLogicalHeight=Math.round(logical.h);imageDimensions.textContent=`${state.previewLogicalWidth} × ${state.previewLogicalHeight}`;previewImage.hidden=true;previewCanvas.hidden=false;setPreviewMode(false);if(textManager){await textManager.setViewport(logical.w,logical.h,previewCanvas);textManager.setInteractive(state.activeTool==='text');textManager.setVisible(true)}}
function scheduleLivePreview(delay=45){clearTimeout(state.renderTimer);invalidateExact();state.renderTimer=setTimeout(()=>renderPreview(),delay)}
function markDirty(){state.panelDirty=true;updateHistoryButtons();scheduleLivePreview()}
function showStudio(){landingView.hidden=true;studioView.hidden=false;newImageButton.hidden=false;renderToolPanel();renderPipeline()}
function clearResult(){revoke(state.resultPreview);state.resultPreview='';state.resultBlob=null;state.exactMode=false}
function resetSourceState(){clearTimeout(state.renderTimer);state.renderToken++;revoke(state.sourceObjectUrl);revoke(state.resultPreview);revoke(state.watermarkPreviewUrl);imageCache.clear();textManager?.clear({silent:true});Object.assign(state,{sourceFile:null,sourceUrl:'',sourceName:'',sourceObjectUrl:'',sourceImage:null,width:0,height:0,activeTool:'resize',pipeline:[],output:{...DEFAULT_OUTPUT},selectedFilter:'vintage',watermarkFile:null,watermarkName:'',watermarkPreviewUrl:'',resizeLock:true,busy:false,panelDirty:false,history:[],historyIndex:-1,restoring:false,resultBlob:null,resultPreview:'',exactMode:false,previewLogicalWidth:0,previewLogicalHeight:0,textFontCategory:'all'})}
function resetStudio(){resetSourceState();previewCanvas.width=1;previewCanvas.height=1;previewCanvas.hidden=true;previewImage.hidden=true;previewImage.removeAttribute('src');textManager?.setVisible(false);downloadButton.disabled=true;compareButton.disabled=true;landingView.hidden=false;studioView.hidden=true;newImageButton.hidden=true;urlInput.value='';fileInput.value='';updateHistoryButtons();updateExport()}
async function loadSource(src,name){try{const img=await loadImage(src);state.sourceImage=img;state.sourceName=name||'image';state.width=img.naturalWidth;state.height=img.naturalHeight;state.pipeline=[];state.output={...DEFAULT_OUTPUT};state.activeTool='resize';state.panelDirty=false;clearResult();imageName.textContent=state.sourceName;downloadButton.disabled=false;compareButton.disabled=false;showStudio();await renderPreview([]);initHistory()}catch{showToast('无法预览这张图片，请检查 URL 或图片权限。','error')}}
function loadLocalFile(file){if(!file||!file.type.startsWith('image/'))return showToast('请选择有效的图片文件。','error');resetSourceState();const url=URL.createObjectURL(file);state.sourceFile=file;state.sourceObjectUrl=url;loadSource(url,file.name||'local-image')}
function loadRemoteUrl(url){let normalized;try{normalized=new URL(url).toString()}catch{return showToast('请输入完整、有效的图片 URL。','error')}resetSourceState();state.sourceUrl=normalized;let name='remote-image';try{name=decodeURIComponent(new URL(normalized).pathname.split('/').pop()||'remote-image')}catch{}loadSource(normalized,name)}
async function setTool(tool){if(!TOOL_META[tool])return;if(state.panelDirty){try{commitDraft(true)}catch(e){showToast(e.message,'error');return}}state.activeTool=tool;$$('.tool-button').forEach(b=>b.classList.toggle('active',b.dataset.tool===tool));textManager?.setInteractive(tool==='text');renderToolPanel()}
const liveHint=(extra='')=>`<div class="live-hint"><span></span><div>修改参数会立即在中间画布预览，无需请求服务器。${extra}</div></div>`;
const actions=()=>`<div class="panel-actions live-actions"><button class="secondary-action" type="button" data-action="reset-param">重置参数</button><button class="primary-action" type="button" data-action="apply">应用此效果</button></div>`;
function resizePanel(){const d=currentLogicalSize(),w=d.w,h=d.h;return `${liveHint()}<div class="field-grid"><label class="field"><span>宽度</span><div class="input-suffix"><input id="resizeWidth" type="number" min="1" value="${w}"><em>px</em></div></label><label class="field"><span>高度</span><div class="input-suffix"><input id="resizeHeight" type="number" min="1" value="${h}"><em>px</em></div></label></div><div class="toggle-line"><span>锁定当前比例</span><button class="toggle ${state.resizeLock?'on':''}" id="ratioToggle" type="button"></button></div><span class="section-label">快速尺寸</span><div class="preset-row"><button class="preset-button" data-size="1920,1080">1920×1080</button><button class="preset-button" data-size="1280,720">1280×720</button><button class="preset-button" data-size="1080,1080">1080×1080</button><button class="preset-button" data-size="800,600">800×600</button></div><span class="section-label">采样方式</span><label class="field"><select id="resizeFilter"><option value="1">Nearest · 最快</option><option value="2">Triangle · 线性</option><option value="3" selected>CatmullRom · 推荐</option><option value="4">Gaussian · 柔和</option><option value="5">Lanczos3 · 高质量</option></select></label>${actions()}`}
function cropPanel(){const d=currentLogicalSize();return `${liveHint('最终导出仍由 Photon 精确裁剪。')}<div class="field-grid"><label class="field"><span>左 X</span><input id="cropX1" type="number" value="0"></label><label class="field"><span>上 Y</span><input id="cropY1" type="number" value="0"></label><label class="field"><span>右 X</span><input id="cropX2" type="number" value="${d.w}"></label><label class="field"><span>下 Y</span><input id="cropY2" type="number" value="${d.h}"></label></div><span class="section-label">常用比例</span><div class="preset-row"><button class="preset-button" data-crop-ratio="1">1:1</button><button class="preset-button" data-crop-ratio="1.333333">4:3</button><button class="preset-button" data-crop-ratio="1.777778">16:9</button><button class="preset-button" data-crop-ratio="0.75">3:4</button><button class="preset-button" data-crop-ratio="0.5625">9:16</button></div>${actions()}`}
function rotatePanel(){return `${liveHint()}<label class="field"><span>旋转角度</span><div class="input-suffix"><input id="rotateAngle" type="number" value="90"><em>°</em></div></label><div class="preset-row"><button class="preset-button" data-angle="90">90°</button><button class="preset-button" data-angle="180">180°</button><button class="preset-button" data-angle="270">270°</button><button class="preset-button" data-angle="-90">-90°</button></div>${actions()}<span class="section-label">快速翻转（立即应用）</span><div class="field-grid"><button class="secondary-action" data-special="fliph">↔ 水平翻转</button><button class="secondary-action" data-special="flipv">↕ 垂直翻转</button></div>`}
function filterPanel(){return `${liveHint('滤镜在浏览器中使用近似视觉预览，精确效果以 Photon 导出结果为准。')}<div class="filter-grid">${FILTERS.map(([id,name])=>`<button class="filter-card ${state.selectedFilter===id?'selected':''}" data-filter="${id}"><span>${name}</span></button>`).join('')}</div><div class="preview-approx">即时滤镜是低延迟近似预览；点击“精确预览”或“导出图片”会使用 Photon 的真实滤镜。</div>${actions()}`}
function adjustPanel(){return `${liveHint()}<span class="section-label">亮度</span><div class="range-row"><input id="brightnessRange" type="range" min="-100" max="100" value="0"><span class="range-value" id="brightnessValue">0</span></div><span class="section-label">对比度</span><div class="range-row"><input id="contrastRange" type="range" min="-100" max="100" value="0"><span class="range-value" id="contrastValue">0</span></div>${actions()}`}
function watermarkPanel(){return `${liveHint()}<span class="section-label">水印图片</span><label class="upload-mini" for="watermarkFile"><strong>${state.watermarkName||'选择本地水印'}</strong><span>${state.watermarkName?'点击可更换文件':'PNG / JPEG / WEBP'}</span></label><input id="watermarkFile" type="file" accept="image/*" hidden><div class="divider"><span>或者使用 URL</span></div><input class="url-watermark-input" id="watermarkUrl" type="url" placeholder="https://.../logo.png"><div class="field-grid" style="margin-top:12px"><label class="field"><span>X 坐标</span><input id="watermarkX" type="number" value="24"></label><label class="field"><span>Y 坐标</span><input id="watermarkY" type="number" value="24"></label></div>${actions()}`}
function escapeHtml(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function rgbaToHex(value,fallback='#000000'){if(!value||!value.startsWith('#'))return fallback;return value.slice(0,7)}
function layerListHtml(){const layers=textManager?.list()||[],active=textManager?.getActiveData();if(!layers.length)return `<div class="empty-card text-empty">还没有文字图层。点击“新增文字”，然后可以直接在画布上拖动、缩放、旋转和双击编辑。</div>`;return `<div class="text-layer-list">${layers.map(layer=>`<div class="text-layer-row ${active?.id===layer.id?'active':''}" data-layer-select="${layer.id}"><div class="layer-main"><span class="layer-dot">T</span><div><strong>${escapeHtml(layer.text||layer.name||'文字')}</strong><small>${layer.fontFamily}</small></div></div><div class="layer-icons"><button data-layer-visible="${layer.id}" title="显示/隐藏">${layer.visible?'◉':'○'}</button><button data-layer-lock="${layer.id}" title="锁定/解锁">${layer.locked?'🔒':'◇'}</button></div></div>`).join('')}</div>`}
function textPanel(){const active=textManager?.getActiveData();const categories=FONT_CATEGORIES.map(c=>`<button class="text-category ${state.textFontCategory===c.id?'active':''}" data-font-category="${c.id}">${c.label}</button>`).join('');const fonts=FONT_OPTIONS.filter(f=>state.textFontCategory==='all'||f.category===state.textFontCategory).map(f=>`<button class="font-card ${active?.fontFamily===f.id?'active':''}" data-font="${escapeHtml(f.id)}" style="font-family:'${escapeHtml(f.id)}',sans-serif"><span>${escapeHtml(f.sample)}</span><small>${escapeHtml(f.label)}</small></button>`).join('');const presets=TEXT_PRESETS.map(p=>`<button class="text-preset ${active?.presetId===p.id?'active':''}" data-text-preset="${p.id}"><span>${escapeHtml(p.name)}</span><small>${escapeHtml(p.description)}</small></button>`).join('');return `<div class="text-tool-actions"><button class="primary-action" data-text-add>＋ 新增文字</button><button class="secondary-action" data-text-duplicate ${active?'':'disabled'}>复制</button><button class="secondary-action danger" data-text-delete ${active?'':'disabled'}>删除</button></div><span class="section-label">图层</span>${layerListHtml()}${active?`<span class="section-label">内容</span><label class="field"><span>文字内容</span><textarea id="textContent" rows="3">${escapeHtml(active.text)}</textarea></label><div class="text-transform-readout" id="textTransformReadout">位置 ${Math.round(active.left)}, ${Math.round(active.top)} · 旋转 ${Math.round(active.angle)}°</div><div class="text-position-actions"><button class="secondary-action" data-text-center>居中</button><button class="secondary-action" data-text-down>下移层级</button><button class="secondary-action" data-text-up>上移层级</button></div><span class="section-label">字体分类</span><div class="text-categories">${categories}</div><div class="font-grid">${fonts}</div><span class="section-label">排版</span><div class="field-grid"><label class="field"><span>字号</span><input id="textFontSize" type="number" min="8" max="420" value="${active.fontSize}"></label><label class="field"><span>字重</span><select id="textFontWeight"><option value="400" ${String(active.fontWeight)==='400'?'selected':''}>Regular</option><option value="500" ${String(active.fontWeight)==='500'?'selected':''}>Medium</option><option value="600" ${String(active.fontWeight)==='600'?'selected':''}>Semibold</option><option value="700" ${String(active.fontWeight)==='700'?'selected':''}>Bold</option><option value="900" ${String(active.fontWeight)==='900'?'selected':''}>Black</option></select></label></div><div class="field-grid"><label class="field"><span>字间距</span><input id="textCharSpacing" type="number" min="-100" max="800" value="${active.charSpacing}"></label><label class="field"><span>行高</span><input id="textLineHeight" type="number" min="0.7" max="3" step="0.05" value="${active.lineHeight}"></label></div><div class="text-align-row"><button data-text-align="left" class="${active.textAlign==='left'?'active':''}">左对齐</button><button data-text-align="center" class="${active.textAlign==='center'?'active':''}">居中</button><button data-text-align="right" class="${active.textAlign==='right'?'active':''}">右对齐</button></div><span class="section-label">颜色与透明度</span><div class="field-grid color-grid"><label class="field"><span>文字颜色</span><input id="textFill" type="color" value="${rgbaToHex(active.fill,'#ffffff')}"></label><label class="field"><span>描边颜色</span><input id="textStroke" type="color" value="${rgbaToHex(active.stroke,'#000000')}"></label></div><span class="section-label">透明度</span><div class="range-row"><input id="textOpacity" type="range" min="0" max="100" value="${Math.round(active.opacity*100)}"><span class="range-value" id="textOpacityValue">${Math.round(active.opacity*100)}%</span></div><span class="section-label">描边宽度</span><div class="range-row"><input id="textStrokeWidth" type="range" min="0" max="16" step="0.5" value="${active.strokeWidth}"><span class="range-value" id="textStrokeWidthValue">${active.strokeWidth}</span></div><span class="section-label">阴影</span><div class="field-grid"><label class="field"><span>颜色</span><input id="textShadowColor" type="color" value="${rgbaToHex(active.shadow?.color,'#000000')}"></label><label class="field"><span>模糊</span><input id="textShadowBlur" type="number" min="0" max="80" value="${active.shadow?.blur||0}"></label><label class="field"><span>偏移 X</span><input id="textShadowX" type="number" min="-80" max="80" value="${active.shadow?.offsetX||0}"></label><label class="field"><span>偏移 Y</span><input id="textShadowY" type="number" min="-80" max="80" value="${active.shadow?.offsetY||0}"></label></div><span class="section-label">角度</span><div class="range-row"><input id="textAngle" type="range" min="-180" max="180" value="${Math.round(active.angle)}"><span class="range-value" id="textAngleValue">${Math.round(active.angle)}°</span></div><span class="section-label">艺术模板</span><div class="preset-grid">${presets}</div><div class="text-tip">提示：双击画布上的文字可以直接编辑；拖动控制点可缩放，顶部控制点可旋转。文字效果会作为透明高清图层参与最终导出。</div>`:`<div class="text-preset-preview"><span class="section-label">快速开始</span><div class="preset-grid">${presets}</div></div>`}`}
function formatPanel(){return `${liveHint('格式与质量不会影响编辑速度，只在精确预览和导出时编码。')}<label class="field"><span>输出格式</span><select id="outputFormat"><option value="webp" ${state.output.format==='webp'?'selected':''}>WEBP · 推荐</option><option value="jpg" ${state.output.format==='jpg'?'selected':''}>JPEG</option><option value="png" ${state.output.format==='png'?'selected':''}>PNG · 无损</option></select></label><span class="section-label">输出质量</span><div class="range-row"><input id="qualityRange" type="range" min="1" max="100" value="${state.output.quality}" ${state.output.format==='png'?'disabled':''}><span class="range-value" id="qualityValue">${state.output.quality}</span></div><div class="exact-note">“导出图片”会自动执行当前全部图片处理与文字图层，无需先点精确预览。</div>`}
function legacyAction(){return state.pipeline.map(o=>`${o.action}${(o.params||[]).length?'!'+o.params.join(','):''}`).join('|')}
function apiUrl(){if(!state.sourceUrl)return'';const u=new URL('/api',location.origin);u.searchParams.set('url',state.sourceUrl);if(state.pipeline.length)u.searchParams.set('action',legacyAction());u.searchParams.set('format',state.output.format);u.searchParams.set('quality',String(state.output.quality));return u.toString()}
function pipelinePanel(){const items=state.pipeline.length?state.pipeline.map((o,i)=>`<div class="pipeline-item"><span class="pipeline-index">${i+1}</span><div><strong>${opName(o.action)}</strong><small>${opSummary(o)}</small></div><div class="pipeline-item-actions"><button data-move="up" data-index="${i}">↑</button><button data-move="down" data-index="${i}">↓</button><button data-remove="${i}">×</button></div></div>`).join(''):'<div class="empty-card">当前还没有图片处理步骤。文字图层独立叠加在最终画面上。</div>';const url=apiUrl();const textCount=textManager?.getObjects().length||0;return `${liveHint('调整流程顺序会立刻重新渲染本地预览；文字图层始终位于图片处理之后。')}<div class="pipeline-list">${items}</div><div class="pipeline-text-summary"><span>T</span><div><strong>${textCount} 个文字图层</strong><small>最终导出时以透明高清 overlay 合成</small></div></div>${url?`<span class="section-label">兼容 API URL</span><div class="code-card">${url}</div><button class="secondary-action" id="copyApiButton" style="width:100%;margin-top:8px">复制 API URL</button>`:''}${state.pipeline.length?'<button class="secondary-action" id="clearPipeline" style="width:100%;margin-top:12px;color:#ffadb6">清空图片处理流程</button>':''}`}
function renderToolPanel(){const [title,badge]=TOOL_META[state.activeTool];toolTitle.textContent=title;toolBadge.textContent=badge;const map={resize:resizePanel,crop:cropPanel,rotate:rotatePanel,filter:filterPanel,adjust:adjustPanel,watermark:watermarkPanel,text:textPanel,format:formatPanel,pipeline:pipelinePanel};toolPanel.innerHTML=map[state.activeTool]();bindPanel();updateExport()}
function centeredCrop(ratio){const d=currentLogicalSize(),cr=d.w/d.h;let cw=d.w,ch=d.h;if(cr>ratio)cw=Math.round(d.h*ratio);else ch=Math.round(d.w/ratio);const x=Math.max(0,Math.round((d.w-cw)/2)),y=Math.max(0,Math.round((d.h-ch)/2));$('#cropX1').value=x;$('#cropY1').value=y;$('#cropX2').value=x+cw;$('#cropY2').value=y+ch;markDirty()}
function opsFromPanel(strict=true){switch(state.activeTool){case'resize':return[{action:'resize',params:[Math.max(1,Math.round(Number($('#resizeWidth').value)||1)),Math.max(1,Math.round(Number($('#resizeHeight').value)||1)),Number($('#resizeFilter').value)||3]}];case'crop':{const values=['#cropX1','#cropY1','#cropX2','#cropY2'].map(id=>Math.round(Number($(id).value)||0));if(values[2]<=values[0]||values[3]<=values[1]){if(strict)throw Error('裁剪区域无效。');return[]}return[{action:'crop',params:values}]}case'rotate':return[{action:'rotate',params:[Number($('#rotateAngle').value)||0]}];case'filter':return[{action:'filter',params:[state.selectedFilter]}];case'adjust':{const b=Number($('#brightnessRange').value)||0,c=Number($('#contrastRange').value)||0,ops=[];if(b)ops.push({action:'adjust_brightness',params:[b]});if(c)ops.push({action:'adjust_contrast',params:[c]});if(!ops.length&&strict)throw Error('亮度与对比度都为 0。');return ops}case'watermark':{const remote=($('#watermarkUrl').value||'').trim(),src=state.watermarkFile?'__watermark__':remote;if(!src){if(strict)throw Error('请上传水印图片或填写 URL。');return[]}return[{action:'watermark',params:[src,Math.max(0,Number($('#watermarkX').value)||0),Math.max(0,Number($('#watermarkY').value)||0)]}]}default:return[]}}
function commitDraft(silent=false){if(!state.panelDirty||!EFFECT_TOOLS.has(state.activeTool))return false;const ops=opsFromPanel(true);if(ops.length)state.pipeline.push(...ops);state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();renderToolPanel();scheduleLivePreview(0);if(!silent)showToast(`已应用 ${ops.map(o=>opName(o.action)).join(' + ')}。`);return true}
function bindTextPanel(){const add=$('[data-text-add]',toolPanel);if(add)add.onclick=async()=>{await textManager.addText({fontFamily:'ZCOOL KuaiLe',fontCategory:'cartoon'});renderToolPanel()};const duplicate=$('[data-text-duplicate]',toolPanel);if(duplicate)duplicate.onclick=()=>textManager.duplicate();const del=$('[data-text-delete]',toolPanel);if(del)del.onclick=()=>textManager.remove();toolPanel.querySelectorAll('[data-layer-select]').forEach(el=>el.onclick=e=>{if(e.target.closest('button'))return;textManager.select(el.dataset.layerSelect)});toolPanel.querySelectorAll('[data-layer-visible]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();textManager.toggleVisible(btn.dataset.layerVisible)});toolPanel.querySelectorAll('[data-layer-lock]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();textManager.toggleLock(btn.dataset.layerLock)});const active=()=>textManager.getActiveData();const content=$('#textContent',toolPanel);if(content)content.oninput=()=>textManager.patchActive({text:content.value,displayName:(content.value||'文字图层').slice(0,12)},{reason:'text'});const center=$('[data-text-center]',toolPanel);if(center)center.onclick=()=>textManager.centerActive();const up=$('[data-text-up]',toolPanel);if(up)up.onclick=()=>textManager.move(active()?.id,'up');const down=$('[data-text-down]',toolPanel);if(down)down.onclick=()=>textManager.move(active()?.id,'down');toolPanel.querySelectorAll('[data-font-category]').forEach(btn=>btn.onclick=()=>{state.textFontCategory=btn.dataset.fontCategory;renderToolPanel()});toolPanel.querySelectorAll('[data-font]').forEach(btn=>btn.onclick=async()=>{const font=fontById(btn.dataset.font);await ensureFontLoaded(font.id);await textManager.patchActive({fontFamily:font.id,fontCategory:font.category,presetId:''},{commit:true,reason:'font'});renderToolPanel()});const numeric=(id,prop,parser=Number)=>{const el=$(id,toolPanel);if(!el)return;el.oninput=()=>textManager.patchActive({[prop]:parser(el.value)},{reason:prop})};numeric('#textFontSize','fontSize',v=>Math.max(8,Number(v)||8));const weight=$('#textFontWeight',toolPanel);if(weight)weight.onchange=()=>textManager.patchActive({fontWeight:Number(weight.value)||400},{commit:true,reason:'weight'});numeric('#textCharSpacing','charSpacing',Number);numeric('#textLineHeight','lineHeight',Number);toolPanel.querySelectorAll('[data-text-align]').forEach(btn=>btn.onclick=()=>textManager.patchActive({textAlign:btn.dataset.textAlign},{commit:true,reason:'align'}));const fill=$('#textFill',toolPanel);if(fill)fill.oninput=()=>textManager.patchActive({fill:fill.value,presetId:''},{reason:'fill'});const stroke=$('#textStroke',toolPanel);if(stroke)stroke.oninput=()=>textManager.patchActive({stroke:stroke.value,presetId:''},{reason:'stroke'});const opacity=$('#textOpacity',toolPanel),opacityValue=$('#textOpacityValue',toolPanel);if(opacity)opacity.oninput=()=>{opacityValue.textContent=`${opacity.value}%`;textManager.patchActive({opacity:Number(opacity.value)/100},{reason:'opacity'})};const strokeWidth=$('#textStrokeWidth',toolPanel),strokeWidthValue=$('#textStrokeWidthValue',toolPanel);if(strokeWidth)strokeWidth.oninput=()=>{strokeWidthValue.textContent=strokeWidth.value;textManager.patchActive({strokeWidth:Number(strokeWidth.value),presetId:''},{reason:'stroke'})};const shadowPatch=()=>{const current=active(),shadow={color:$('#textShadowColor')?.value||current?.shadow?.color||'#000000',blur:Number($('#textShadowBlur')?.value)||0,offsetX:Number($('#textShadowX')?.value)||0,offsetY:Number($('#textShadowY')?.value)||0};textManager.patchActive({shadow,presetId:''},{reason:'shadow'})};['#textShadowColor','#textShadowBlur','#textShadowX','#textShadowY'].forEach(id=>{const el=$(id,toolPanel);if(el)el.oninput=shadowPatch});const angle=$('#textAngle',toolPanel),angleValue=$('#textAngleValue',toolPanel);if(angle)angle.oninput=()=>{angleValue.textContent=`${angle.value}°`;textManager.patchActive({angle:Number(angle.value)},{reason:'angle'})};toolPanel.querySelectorAll('[data-text-preset]').forEach(btn=>btn.onclick=async()=>{let target=textManager.getActive();const preset=TEXT_PRESETS.find(p=>p.id===btn.dataset.textPreset);if(!target){await textManager.addText({fontFamily:preset?.style?.fontFamily||'ZCOOL KuaiLe'});target=textManager.getActive()}await textManager.applyPreset(preset);renderToolPanel()})}
function updateTextTransformReadout(){const active=textManager?.getActiveData(),el=$('#textTransformReadout');if(active&&el)el.textContent=`位置 ${Math.round(active.left)}, ${Math.round(active.top)} · 旋转 ${Math.round(active.angle)}°`}
function bindPanel(){if(state.activeTool==='text'){bindTextPanel();return}const resetParam=$('[data-action="reset-param"]',toolPanel);if(resetParam)resetParam.onclick=()=>{state.panelDirty=false;renderToolPanel();scheduleLivePreview(0);updateHistoryButtons()};const apply=$('[data-action="apply"]',toolPanel);if(apply)apply.onclick=()=>{try{commitDraft()}catch(e){showToast(e.message,'error')}};toolPanel.querySelectorAll('[data-size]').forEach(btn=>btn.onclick=()=>{const [w,h]=btn.dataset.size.split(',');$('#resizeWidth').value=w;$('#resizeHeight').value=h;markDirty()});const ratioToggle=$('#ratioToggle',toolPanel);if(ratioToggle){ratioToggle.onclick=()=>{state.resizeLock=!state.resizeLock;ratioToggle.classList.toggle('on',state.resizeLock)};const w=$('#resizeWidth'),h=$('#resizeHeight');const d=currentLogicalSize(),ratio=d.h?d.w/d.h:1;w.oninput=()=>{if(state.resizeLock&&ratio)h.value=Math.max(1,Math.round(Number(w.value)/ratio));markDirty()};h.oninput=()=>{if(state.resizeLock&&ratio)w.value=Math.max(1,Math.round(Number(h.value)*ratio));markDirty()};$('#resizeFilter').onchange=markDirty}toolPanel.querySelectorAll('[data-crop-ratio]').forEach(btn=>btn.onclick=()=>centeredCrop(Number(btn.dataset.cropRatio)));['#cropX1','#cropY1','#cropX2','#cropY2'].forEach(id=>{const el=$(id,toolPanel);if(el)el.oninput=markDirty});toolPanel.querySelectorAll('[data-angle]').forEach(btn=>btn.onclick=()=>{$('#rotateAngle').value=btn.dataset.angle;markDirty()});const angle=$('#rotateAngle',toolPanel);if(angle)angle.oninput=markDirty;toolPanel.querySelectorAll('[data-special]').forEach(btn=>btn.onclick=()=>{state.pipeline.push({action:btn.dataset.special,params:[]});state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();renderToolPanel();scheduleLivePreview(0);showToast(`已应用${opName(btn.dataset.special)}。`)});toolPanel.querySelectorAll('[data-filter]').forEach(btn=>btn.onclick=()=>{state.selectedFilter=btn.dataset.filter;toolPanel.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('selected',x===btn));markDirty()});[['brightnessRange','brightnessValue'],['contrastRange','contrastValue']].forEach(([r,v])=>{const input=$('#'+r),value=$('#'+v);if(input&&value)input.oninput=()=>{value.textContent=input.value;markDirty()}});const wm=$('#watermarkFile');if(wm)wm.onchange=()=>{const file=wm.files[0];if(file){revoke(state.watermarkPreviewUrl);state.watermarkFile=file;state.watermarkName=file.name;state.watermarkPreviewUrl=URL.createObjectURL(file);state.panelDirty=true;renderToolPanel();scheduleLivePreview(0);updateHistoryButtons()}};const wmUrl=$('#watermarkUrl');if(wmUrl)wmUrl.oninput=markDirty;['#watermarkX','#watermarkY'].forEach(id=>{const el=$(id,toolPanel);if(el)el.oninput=markDirty});const format=$('#outputFormat');if(format)format.onchange=()=>{state.output.format=format.value;updateExport();pushHistory();renderToolPanel();invalidateExact()};const quality=$('#qualityRange');if(quality){const value=$('#qualityValue');quality.oninput=()=>{state.output.quality=Math.max(1,Math.min(100,Number(quality.value)||92));value.textContent=state.output.quality;updateExport();invalidateExact()};quality.onchange=pushHistory}toolPanel.querySelectorAll('[data-remove]').forEach(btn=>btn.onclick=()=>{state.pipeline.splice(Number(btn.dataset.remove),1);state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();renderToolPanel();scheduleLivePreview(0)});toolPanel.querySelectorAll('[data-move]').forEach(btn=>btn.onclick=()=>{const i=Number(btn.dataset.index),j=btn.dataset.move==='up'?i-1:i+1;if(j<0||j>=state.pipeline.length)return;[state.pipeline[i],state.pipeline[j]]=[state.pipeline[j],state.pipeline[i]];state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();renderToolPanel();scheduleLivePreview(0)});const clear=$('#clearPipeline');if(clear)clear.onclick=()=>{state.pipeline=[];state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();renderToolPanel();scheduleLivePreview(0)};const copy=$('#copyApiButton');if(copy)copy.onclick=async()=>{await navigator.clipboard.writeText(apiUrl());showToast('API URL 已复制。')}}
function renderPipeline(){pipelineCount.textContent=`${state.pipeline.length} 个图片步骤 · ${textManager?.getObjects().length||0} 个文字图层`;const committed=state.pipeline.map((o,i)=>`<div class="pipeline-chip"><span>${i+1}. ${opName(o.action)} <small>${opSummary(o)}</small></span><button data-chip-remove="${i}">×</button></div>`);let draft=[];if(state.panelDirty&&EFFECT_TOOLS.has(state.activeTool)){try{const ops=opsFromPanel(false);draft=ops.length?[`<div class="pipeline-chip draft"><span>预览中 · ${ops.map(o=>opName(o.action)).join(' + ')}</span></div>`]:[]}catch{}}const textCount=textManager?.getObjects().length||0;if(textCount)committed.push(`<div class="pipeline-chip text-chip"><span>T · 文字图层 <small>${textCount} 个</small></span></div>`);pipelineChips.innerHTML=committed.length||draft.length?[...committed,...draft].join(''):'<span class="empty-pipeline">参数调整会即时预览；文字图层可直接在画布编辑</span>';pipelineChips.querySelectorAll('[data-chip-remove]').forEach(btn=>btn.onclick=()=>{state.pipeline.splice(Number(btn.dataset.chipRemove),1);state.panelDirty=false;invalidateExact();pushHistory();renderPipeline();if(state.activeTool==='pipeline')renderToolPanel();scheduleLivePreview(0)})}
async function serverRender({download=false}={}){if(!state.sourceImage||state.busy)return;try{if(state.panelDirty)commitDraft(true)}catch(e){showToast(e.message,'error');return}setBusy(true);try{const finalSize=currentLogicalSize(),overlay=await textManager?.exportOverlay(finalSize.w,finalSize.h);const form=new FormData();if(state.sourceFile)form.append('file',state.sourceFile);if(state.sourceUrl)form.append('url',state.sourceUrl);form.append('pipeline',JSON.stringify(state.pipeline));form.append('format',state.output.format);form.append('quality',String(state.output.quality));if(state.watermarkFile&&state.pipeline.some(o=>o.action==='watermark'&&o.params?.[0]==='__watermark__'))form.append('watermark',state.watermarkFile);if(overlay)form.append('textOverlay',overlay,'text-overlay.png');const response=await fetch('/api/studio',{method:'POST',body:form});if(!response.ok){let message=`处理失败（HTTP ${response.status}）`;try{const data=await response.json();if(data.error)message=data.error}catch{}throw Error(message)}const blob=await response.blob();revoke(state.resultPreview);state.resultBlob=blob;state.resultPreview=URL.createObjectURL(blob);previewImage.onload=()=>{imageDimensions.textContent=`${previewImage.naturalWidth} × ${previewImage.naturalHeight}`};previewImage.src=state.resultPreview;previewImage.hidden=false;previewCanvas.hidden=true;textManager?.setVisible(false);setPreviewMode(true);compareButton.disabled=false;if(download){const ext=state.output.format==='jpg'?'jpg':state.output.format,base=(state.sourceName||'image').replace(/\.[a-z0-9]+$/i,'').replace(/[^\w\u4e00-\u9fff-]+/g,'-'),a=document.createElement('a');a.href=state.resultPreview;a.download=`${base||'image'}-processed.${ext}`;document.body.appendChild(a);a.click();a.remove();showToast(`导出完成 · ${Math.max(1,Math.round(blob.size/1024))} KB`)}else showToast(`精确预览完成 · ${Math.max(1,Math.round(blob.size/1024))} KB`)}catch(e){showToast(e.message||'图片处理失败。','error')}finally{setBusy(false)}}
function showOriginal(){if(!state.sourceImage)return;previewImage.src=state.sourceObjectUrl||state.sourceUrl;previewImage.hidden=false;previewCanvas.hidden=true;textManager?.setVisible(false);compareButton.textContent='正在显示原图'}
function restorePreview(){compareButton.textContent='按住看原图';if(state.exactMode&&state.resultPreview){previewImage.src=state.resultPreview;previewImage.hidden=false;previewCanvas.hidden=true;textManager?.setVisible(false)}else{previewImage.hidden=true;previewCanvas.hidden=false;textManager?.setVisible(true);textManager?.syncTo(previewCanvas);imageDimensions.textContent=`${state.previewLogicalWidth||state.width} × ${state.previewLogicalHeight||state.height}`}}
chooseFileButton.onclick=()=>fileInput.click();fileInput.onchange=()=>loadLocalFile(fileInput.files[0]);urlForm.onsubmit=e=>{e.preventDefault();loadRemoteUrl(urlInput.value.trim())};['dragenter','dragover'].forEach(name=>dropZone.addEventListener(name,e=>{e.preventDefault();dropZone.classList.add('dragging')}));['dragleave','drop'].forEach(name=>dropZone.addEventListener(name,e=>{e.preventDefault();dropZone.classList.remove('dragging')}));dropZone.addEventListener('drop',e=>loadLocalFile(e.dataTransfer.files?.[0]));$$('.tool-button').forEach(btn=>btn.onclick=()=>setTool(btn.dataset.tool));runButton.onclick=()=>serverRender({download:false});downloadButton.onclick=()=>serverRender({download:true});newImageButton.onclick=resetStudio;undoButton.onclick=undo;redoButton.onclick=redo;resetButton.onclick=resetEdits;fitButton.onclick=()=>{canvasStage.classList.toggle('cover');fitButton.textContent=canvasStage.classList.contains('cover')?'留出边距':'适应窗口';setTimeout(()=>textManager?.syncTo(previewCanvas),40)};compareButton.onpointerdown=showOriginal;compareButton.onpointerup=restorePreview;compareButton.onpointerleave=restorePreview;window.addEventListener('pointerup',restorePreview);window.addEventListener('resize',()=>{if(!state.exactMode)textManager?.syncTo(previewCanvas)});window.addEventListener('keydown',e=>{const tag=e.target?.tagName;if(['INPUT','TEXTAREA','SELECT'].includes(tag))return;const mod=e.ctrlKey||e.metaKey;if(mod&&e.key.toLowerCase()==='z'){e.preventDefault();e.shiftKey?redo():undo()}else if(mod&&e.key.toLowerCase()==='y'){e.preventDefault();redo()}else if((e.key==='Delete'||e.key==='Backspace')&&state.activeTool==='text'&&textManager?.getActive()){e.preventDefault();textManager.remove()}});
previewCanvas.hidden=true;previewImage.hidden=true;textManager?.setVisible(false);updateHistoryButtons();updateExport();setPreviewMode(false);
